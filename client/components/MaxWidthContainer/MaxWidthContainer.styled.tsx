import styled from 'styled-components'
import { maxWidth } from '../../global-styles/variables'

export const MaxWidthContainerWrapper = styled.div`
  width: 100%;
  height: 100%;
  max-width: ${maxWidth}px;
  margin: 0 auto;
`
